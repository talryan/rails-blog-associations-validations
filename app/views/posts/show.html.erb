<p id="notice"><%= notice %></p>

<p>
  <h3>Name:</h3>
  <%= @post.name %><br>
  <%= @post.content %><br>
  <ul>
  <% @post.tags.each do |t| %>
    <%= t.name %>
  <% end %>
  </ul>
</p>


<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>